import React, { Component } from 'react';
import LoginCard from '../components/LoginCard';

export default class Login extends Component {
  render() {
    return (
      <section><LoginCard /></section>
    );
  }
}
